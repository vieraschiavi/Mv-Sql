{
  "name": "mv-sql-nlp-web",
  "private": true,
  "version": "1.0.0",
  "description": "MV SQL NLP — landing + funciones serverless (MercadoPago + descarga con licencia)",
  "dependencies": {
    "jsonwebtoken": "^9.0.3",
    "jszip": "^3.10.1",
    "mercadopago": "^2.0.9"
  },
  "optionalDependencies": {
    "@vercel/kv": "^2.0.0"
  },
  "scripts": {
    "test": "node ../tools/correr-tests.mjs --web"
  }
}
